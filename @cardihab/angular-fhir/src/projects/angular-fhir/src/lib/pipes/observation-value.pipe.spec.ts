import { ObservationValuePipe } from './observation-value.pipe';

describe('ObservationValuePipe', () => {
  it('create an instance', () => {
    const pipe = new ObservationValuePipe();
    expect(pipe).toBeTruthy();
  });
});
